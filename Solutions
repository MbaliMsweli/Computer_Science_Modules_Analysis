---Solutions for the Computer Science Database

USE computer_sciences


-- 1. Write a query to display all rows from the course_curriculum table.
SELECT* 
FROM [dbo].[course_curriculum]

-- 2. Write a query to show only the Module_Code and Module_Description columns.
SELECT 
	Module_Code,
	Module_Description
FROM [dbo].[course_curriculum]

-- 3. Write a query to display the Module_Code and Module_Credits for all modules.
SELECT 
	Module_Code,
	Module_Credits
FROM [dbo].[course_curriculum]

-- 4. Write a query to find all modules that are Core modules. **Why is IN operator not working?**
SELECT*
FROM [dbo].[course_curriculum]
WHERE Module_Type = 'Core module'


-- 5. Write a query to list all Elective modules.
SELECT*
FROM [dbo].[course_curriculum]
WHERE Module_Type = 'Elective module'


-- 6. Write a query to find all modules with more than 15 credits.
SELECT*
FROM [dbo].[course_curriculum]
WHERE Module_Credits = 15

-- 7. Write a query to find the module with the code 'COS 216'.
SELECT*
FROM [dbo].[course_curriculum]
WHERE Module_Code = 'COS 216'


-- 8. Write a query to display modules where the description contains the word 'Statistics'.
SELECT*
FROM [dbo].[course_curriculum]
WHERE Module_Description LIKE '%Statistics%'

-- 9. Write a query to show all modules with credits equal to 12.
SELECT*
FROM [dbo].[course_curriculum]
WHERE Module_Credits = 12

-- 10. Write a query to sort all modules by Module_Credits in descending order.
SELECT*
FROM [dbo].[course_curriculum]
ORDER BY Module_Credits DESC


-- 11. Write a query to sort modules alphabetically by Module_Code.
SELECT*
FROM [dbo].[course_curriculum]
ORDER BY Module_Code ASC

-- 12. Write a query to sort Elective modules by Module_Credits in ascending order.
SELECT*
FROM [dbo].[course_curriculum]
WHERE Module_Type = 'Elective Module' 
ORDER BY Module_Credits ASC

-- 13. Write a query to count how many total modules are in the curriculum.
SELECT 
	COUNT(DISTINCT Course_Id) AS Total_Modules
	FROM [dbo].[course_curriculum]

-- 14. Write a query to calculate the total credits for all modules.
SELECT 
	SUM(Module_Credits) As Total_Credits
FROM [dbo].[course_curriculum]

-- 15. Write a query to find the highest number of credits in the table.
SELECT 
	MAX(Module_Credits) AS Higest_Credit
FROM [dbo].[course_curriculum]

-- 16. Write a query to find the lowest number of credits in the table.
SELECT 
	MIN(Module_Credits) AS Lowest_Credit
FROM [dbo].[course_curriculum]

-- 17. Write a query to count how many modules exist in each Module_Type.
SELECT 
	Module_Type,
	COUNT(DISTINCT Module_Code) AS No_of_Modules
FROM [dbo].[course_curriculum]
GROUP BY Module_Type

-- 18. Write a query to calculate the average credits for each module type.
SELECT Module_Type,
	AVG(Module_Credits) AS Avg_Credits
FROM [dbo].[course_curriculum]
GROUP BY Module_Type

-- 19. Write a query to display module types where the total credits exceed 30.
SELECT Module_Type,
	SUM(Module_Credits) AS Total_Credits
FROM [dbo].[course_curriculum]
GROUP BY Module_Type
HAVING SUM (Module_Credits) >30 

-- 20. Write a query to list all modules whose module code starts with 'COS'.
SELECT* 
FROM [dbo].[course_curriculum]
WHERE Module_Code LIKE 'COS%'

-- 21. Write a query to list all modules whose description contains '212'.
SELECT* 
FROM [dbo].[course_curriculum]
WHERE Module_Code LIKE 'COS%'


-- 22. Write a query to show all module descriptions that contain the word 'project'.
SELECT* 
FROM [dbo].[course_curriculum]
WHERE Module_Description LIKE '%project%'


-- 23. Write a query to correct the spelling of 'Netcentric computer sysytems 216' to 'Netcentric computer systems 216'.
UPDATE [dbo].[course_curriculum]
SET Module_Description = 'Netcentric computer systems 216'
WHERE Course_Id = 4

-- 24. Write a query to change the credits of COS 212 to 20 credits.
UPDATE [dbo].[course_curriculum]
SET Module_Credits = 20
WHERE Module_Code= 'COS 212'

-- 25. Write a query to change all Elective modules to Core modules.
UPDATE [dbo].[course_curriculum]
SET  Module_Type = 'Core Module'
WHERE Module_Type = 'Elective Module'

-- 26. Write a query to delete the module 'JCP 202' from the table.
UPDATE [dbo].[course_curriculum]
SET Module_Code = NULL
WHERE Course_Id = 1

-- 27. Write a query to delete all modules with credits less than 10. **changed to <=16
DELETE
FROM [dbo].[course_curriculum]
WHERE	(Module_credits) <= 16


-- 28. Write a query to delete all Fundamental modules.**querried before no:27
DELETE 
FROM [dbo].[course_curriculum]
WHERE Module_Type = 'Fundamental module'

-- 29. Write an INSERT statement to add the module (COS 300, 'Advanced Database Systems', 20, 'Core module').
INSERT INTO [dbo].[course_curriculum]
	(Module_Code,Module_Description,Module_Credits,Module_Type)
VALUES
	('COS 300','Advanced Database Systems',20,'Core module')
	

-- 30. Write one INSERT statement to add:
--     (COS 310, 'Machine Learning 310', 24, 'Core module')
--     (STK 230, 'Business Statistics 230', 20, 'Elective module').
INSERT INTO [dbo].[course_curriculum]
	(Module_Code,Module_Description,Module_Credits,Module_Type)
VALUES
	('COS 310', 'Machine Learning 310', 24, 'Core module'),
	('STK 230', 'Business Statistics 230', 20, 'Elective module')
	

